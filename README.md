# Parquet Under the Hood

Reproducible byte-level teardown of Apache Parquet: a richly-typed sample file,
a footer/metadata walkthrough, and the small-vs-large size tradeoff — all from
`make all`.

## Quickstart
```bash
uv sync
make all
```

Full write-up: <link to your blog post once live>